name: Build and Deploy createUser Service

on:
  push:
    paths:
      - 'backend/User/createUser/**'
    branches:
      - bd_microservices

  pull_request:
    paths:
      - 'backend/User/createUser/**'
    branches:
      - "QA"
      - "main"

jobs:
  build-and-push:
    name: 📦 Build Docker Image and Push to Docker Hub
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Clone the repository
        uses: actions/checkout@v3

      - name: 🐳 Log in to Docker Hub
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: 🛠️ Build Docker Image
        run: |
          docker build --build-arg DATABASE_URL=${{ secrets.DATABASE_URL }} -t ${{ secrets.DOCKER_USERNAME }}/createUser:latest backend/User/createUser
          docker tag ${{ secrets.DOCKER_USERNAME }}/createUser:latest ${{ secrets.DOCKER_USERNAME }}/createUser:${{ github.sha }}

      - name: 📤 Upload image to Docker Hub
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/createUser:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/createUser:${{ github.sha }}

  deploy-to-ec2:
    name: 🚀 Deploy createUser Service on AWS EC2
    runs-on: ubuntu-latest
    needs: build-and-push

    steps:
      - name: 🔄 Deploy
        run: |
          ssh -o StrictHostKeyChecking=no -i ${{ secrets.EC2_SSH_KEY }} ubuntu@${{ secrets.EC2_PUBLIC_IP }} << 'EOF'
            sudo docker pull ${{ secrets.DOCKER_USERNAME }}/createUser:latest
            sudo docker stop createUser || true
            sudo docker rm createUser || true
            sudo docker run -d --name createUser -p 8094:8094 \
              -e DATABASE_URL=${{ secrets.DATABASE_URL }} \
              ${{ secrets.DOCKER_USERNAME }}/createUser:latest
          EOF
