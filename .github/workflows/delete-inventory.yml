name: Build and Deploy Delete-Inventory Service

on:
  push:
    paths:
      - 'backend/Inventories/delete-inventories/**'
    branches:
      - Yamel_inventory

  pull_request:
    paths:
      - 'backend/Inventories/delete-inventories/**'
    branches:
      - "QA"
      - "main"

jobs:
  build-and-push:
    name: 📦 Build Docker Image and Push to Docker Hub
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Clone the repository
        uses: actions/checkout@v3

      - name: 🐳 Log in to Docker Hub
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: 🛠️ Build Docker Image
        run: |
          docker build --build-arg DATABASE_URL=${{ secrets.DATABASE_URL }} -t ${{ secrets.DOCKER_USERNAME }}/delete-inventory:latest backend/Inventories/delete-inventories
          docker tag ${{ secrets.DOCKER_USERNAME }}/delete-inventory:latest ${{ secrets.DOCKER_USERNAME }}/delete-inventory:${{ github.sha }}

      - name: 📤 Upload image to Docker Hub
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/delete-inventory:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/delete-inventory:${{ github.sha }}

  deploy-to-ec2:
    name: 🚀 Deploy on AWS EC2
    runs-on: ubuntu-latest
    needs: build-and-push

    steps:
      - name: 🔄 Deploy
        run: |
          ssh -o StrictHostKeyChecking=no -i ${{ secrets.EC2_SSH_KEY }} ubuntu@${{ secrets.EC2_PUBLIC_IP }} << 'EOF'
            sudo docker pull ${{ secrets.DOCKER_USERNAME }}/delete-inventory:latest
            sudo docker stop delete-inventory || true
            sudo docker rm delete-inventory || true
            sudo docker run -d --name delete-inventory -p 5002:5002 \
              -e DATABASE_URL=${{ secrets.DATABASE_URL }} \
              ${{ secrets.DOCKER_USERNAME }}/delete-inventory:latest
          EOF
