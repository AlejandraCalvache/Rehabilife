name: Build and Deploy deleteProduct Service

on:
  push:
    paths:
      - 'backend/Products/deleteProduct/**'
    branches:
      - bd_microservices

  pull_request:
    paths:
      - 'backend/Products/deleteProduct/**'
    branches:
      - "QA"
      - "main"

jobs:
  build-and-push:
    name: 📦 Build Docker Image and Push to Docker Hub
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Clone the repository
        uses: actions/checkout@v3

      - name: 🐳 Log in to Docker Hub
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: 🛠️ Build Docker Image
        run: |
          docker build --build-arg DATABASE_URL=${{ secrets.DATABASE_URL }} -t ${{ secrets.DOCKER_USERNAME }}/deleteProduct:latest backend/Products/deleteProduct
          docker tag ${{ secrets.DOCKER_USERNAME }}/deleteProduct:latest ${{ secrets.DOCKER_USERNAME }}/deleteProduct:${{ github.sha }}

      - name: 📤 Upload image to Docker Hub
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/deleteProduct:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/deleteProduct:${{ github.sha }}

  deploy-to-ec2:
    name: 🚀 Deploy deleteProduct Service on AWS EC2
    runs-on: ubuntu-latest
    needs: build-and-push

    steps:
      - name: 🔄 Deploy
        run: |
          ssh -o StrictHostKeyChecking=no -i ${{ secrets.EC2_SSH_KEY }} ubuntu@${{ secrets.EC2_PUBLIC_IP }} << 'EOF'
            sudo docker pull ${{ secrets.DOCKER_USERNAME }}/deleteProduct:latest
            sudo docker stop deleteProduct || true
            sudo docker rm deleteProduct || true
            sudo docker run -d --name deleteProduct -p 8082:8082 \
              -e DATABASE_URL=${{ secrets.DATABASE_URL }} \
              ${{ secrets.DOCKER_USERNAME }}/deleteProduct:latest
          EOF
